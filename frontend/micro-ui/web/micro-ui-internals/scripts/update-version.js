#!/usr/bin/env node
// update-version.js disabled: static versioning is handled in src/versionConfig.js
// Keeping this file to avoid breaking Dockerfiles/scripts that invoke it.
console.log('[update-version] Skipped. Using static versionConfig.js for cache management.');