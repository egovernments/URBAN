// Static version configuration (manually maintained)
// Update this value whenever you want to invalidate caches across clients

const VersionConfig = {
  version: "1.0.0-manual", // <-- update manually
};

export default VersionConfig;


